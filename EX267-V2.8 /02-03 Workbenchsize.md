🚀 Change the defaul WorkBench Size.

Ensure the **`Large and X Large`** sizes are no longer available as choices to users when creating new Workbenches.
- Ensure there is a new `My-medium` workbench size available with **limited** to 2 `CPUs` and 2Gi of `Memory`, but requests 1 `CPU` and 1Gi of `memory`.
