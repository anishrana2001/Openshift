## Registary URL: registry.ocp4.example.com:8443
## One can use users name `developer` and same password.


- One can use the S3 URL `https://minio-ui-minio.apps.ocp4.example.com`.
- `admin` user can use the URL `https://api.ocp4.example.com:6443`
- The password of RHOC is `redhatocp`.
- S3 credentials is `minio` and password is `minio123`
