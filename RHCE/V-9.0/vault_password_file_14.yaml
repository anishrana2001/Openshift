#!/bin/bash

# Set your variable and value
VAR_NAME1="pw_manager"
VAR_VALUE1="MySecretManagerPassword"
VAR_NAME2="pv_engineer"
VAR_VALUE2="loginfornodes"
VAULT_FILE="/home/student/ansible/vault_password_file_14.yaml"
VAULT_PASS_FILE="/home/student/ansible/vault_password_file_14.yaml"

# Create the vault file using encrypt_string and redirect to locker.yml
ansible-vault encrypt_string "$VAR_VALUE1" --name "$VAR_NAME1" --vault-password-file "$VAULT_PASS_FILE" > "$VAULT_FILE"
ansible-vault encrypt_string "$VAR_VALUE2" --name "$VAR_NAME2" --vault-password-file "$VAULT_PASS_FILE" >> "$VAULT_FILE"
